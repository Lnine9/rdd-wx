
export default {
	state: {
	    /** token */
	    token: null,
	    /** 用户登陆角色信息 */
	    // userInfo: Auth.getUserInfo() || {},
	    // /** 用户登陆账户密码 */
	    // accountPwd: Auth.getAccountPwd() || defaultUser,
	    // /** 是否登陆 */
	    // isLogin: Auth.getLogin() || false,
	  }
}